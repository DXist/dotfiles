# Tmuxp workspace config
# See http://tmuxp.readthedocs.io/en/latest/about.html for details
session_name: example
start_directory: $PROJECT_HOME/example

windows:
  - window_name: server

    layout: even-horizontal
    panes:
      - shell_command:
      - shell_command:

  - window_name: vim
    focus: True
    panes:
      - shell_command:
        - vim

  - window_name: ''
    layout: even-horizontal
    panes:
      - shell_command:
